/**
 * ═══════════════════════════════════════════════════════════════════
 * 🧪 سكريبت اختبار نظام تسليم أجهزة الصيانة
 * استخدم هذا السكريبت للتحقق من عمل جميع الوظائف
 * ═══════════════════════════════════════════════════════════════════
 * 
 * كيفية الاستخدام:
 * 1. افتح Console في المتصفح (اضغط F12)
 * 2. انسخ هذا الكود بالكامل
 * 3. الصقه في Console واضغط Enter
 * 4. شاهد النتائج
 */

(function() {
    console.log('═══════════════════════════════════════════════════════════════');
    console.log('🧪 بدء اختبار نظام تسليم أجهزة الصيانة');
    console.log('═══════════════════════════════════════════════════════════════');
    
    let passedTests = 0;
    let failedTests = 0;
    const warnings = [];
    
    function test(name, condition, warning = null) {
        if (condition) {
            console.log(`✅ PASS: ${name}`);
            passedTests++;
        } else {
            console.error(`❌ FAIL: ${name}`);
            failedTests++;
            if (warning) warnings.push(warning);
        }
    }
    
    // ═══════════════════════════════════════════════════════════════
    // 1. اختبار الدوال الأساسية
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n📋 1. اختبار الدوال الأساسية:');
    console.log('─────────────────────────────────────────────────────────────');
    
    test('window.completeDelivery موجودة', 
         typeof window.completeDelivery === 'function',
         'تأكد من تحميل ملف maintenance-delivery-fix.js');
    
    test('window.showDeliveryModal موجودة', 
         typeof window.showDeliveryModal === 'function',
         'تأكد من تحميل ملف maintenance-delivery-fix.js');
    
    test('window.completeMaintenance موجودة', 
         typeof window.completeMaintenance === 'function',
         'تأكد من تحميل ملف maintenance-delivery-fix.js');
    
    test('window.closeMaintenanceModal موجودة', 
         typeof window.closeMaintenanceModal === 'function',
         'قد تكون معرفة في السكريبت الأصلي');
    
    // ═══════════════════════════════════════════════════════════════
    // 2. اختبار البيانات
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n💾 2. اختبار البيانات:');
    console.log('─────────────────────────────────────────────────────────────');
    
    test('window.maintenanceData موجودة', 
         window.maintenanceData !== undefined,
         'قد تحتاج لفتح صفحة الصيانة أولاً');
    
    test('window.deliveredDevicesData موجودة', 
         window.deliveredDevicesData !== undefined,
         'البيانات ستُنشأ عند أول تسليم');
    
    const maintenanceCount = window.maintenanceData ? window.maintenanceData.length : 0;
    const deliveredCount = window.deliveredDevicesData ? window.deliveredDevicesData.length : 0;
    
    console.log(`   📊 عدد الأجهزة قيد الصيانة: ${maintenanceCount}`);
    console.log(`   📊 عدد الأجهزة المسلمة: ${deliveredCount}`);
    
    // ═══════════════════════════════════════════════════════════════
    // 3. اختبار localStorage
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n💿 3. اختبار localStorage:');
    console.log('─────────────────────────────────────────────────────────────');
    
    test('localStorage متاح', 
         typeof localStorage !== 'undefined',
         'localStorage ضروري لحفظ البيانات');
    
    if (typeof localStorage !== 'undefined') {
        const maintenanceDataLS = localStorage.getItem('maintenanceData');
        const deliveredDevicesDataLS = localStorage.getItem('deliveredDevicesData');
        
        test('بيانات الصيانة في localStorage',
             maintenanceDataLS !== null,
             'قد تحتاج لإضافة بعض الأجهزة أولاً');
        
        console.log(`   📊 حجم بيانات الصيانة: ${maintenanceDataLS ? maintenanceDataLS.length : 0} حرف`);
        console.log(`   📊 حجم بيانات الأجهزة المسلمة: ${deliveredDevicesDataLS ? deliveredDevicesDataLS.length : 0} حرف`);
    }
    
    // ═══════════════════════════════════════════════════════════════
    // 4. اختبار عناصر الواجهة
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n🎨 4. اختبار عناصر الواجهة:');
    console.log('─────────────────────────────────────────────────────────────');
    
    test('صفحة الصيانة موجودة', 
         document.getElementById('maintenance') !== null,
         'انتقل إلى صفحة إدارة الصيانة');
    
    test('جدول الصيانة موجود',
         document.getElementById('maintenanceTableBody') !== null,
         'تأكد من فتح صفحة الصيانة');
    
    // ═══════════════════════════════════════════════════════════════
    // 5. اختبار وظيفي (إذا كانت البيانات متوفرة)
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n⚙️ 5. اختبار وظيفي:');
    console.log('─────────────────────────────────────────────────────────────');
    
    if (window.maintenanceData && window.maintenanceData.length > 0) {
        const testDevice = window.maintenanceData[0];
        console.log(`   🔧 جهاز اختباري: ${testDevice.productName}`);
        
        test('الجهاز له ID صالح',
             testDevice.id !== undefined && testDevice.id !== null,
             'البيانات قد تكون تالفة');
        
        test('الجهاز له اسم',
             testDevice.productName !== undefined && testDevice.productName !== '',
             'بيانات الجهاز غير كاملة');
        
        test('الجهاز له سعر',
             testDevice.price !== undefined && !isNaN(testDevice.price),
             'السعر مطلوب لعملية التسليم');
        
        // محاكاة فتح نافذة التسليم (بدون فتح فعلي)
        console.log('   🧪 محاولة محاكاة فتح نافذة التسليم...');
        try {
            // لا نستدعي الدالة فعلياً لتجنب فتح النافذة
            // فقط نتحقق من إمكانية الاستدعاء
            const canCall = typeof window.showDeliveryModal === 'function';
            test('يمكن استدعاء showDeliveryModal',
                 canCall,
                 'الدالة غير متاحة');
            
            if (canCall) {
                console.log('   ✅ يمكن فتح نافذة التسليم لهذا الجهاز');
            }
        } catch (error) {
            test('استدعاء showDeliveryModal بدون أخطاء', false);
            console.error('   ❌ خطأ:', error.message);
        }
    } else {
        console.warn('   ⚠️ لا توجد أجهزة للاختبار');
        console.warn('   💡 أضف بعض الأجهزة للصيانة لإجراء اختبار كامل');
    }
    
    // ═══════════════════════════════════════════════════════════════
    // 6. اختبار دوال المساعدة
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n🔧 6. اختبار دوال المساعدة:');
    console.log('─────────────────────────────────────────────────────────────');
    
    test('showNotification موجودة',
         typeof showNotification === 'function' || 
         typeof window.showNotification === 'function' ||
         typeof window.showToast === 'function',
         'قد تكون معرفة بأسماء مختلفة');
    
    test('formatCurrency موجودة أو قابلة للتطبيق',
         typeof formatCurrency === 'function' || 
         typeof Intl !== 'undefined',
         'لتنسيق العملة');
    
    test('formatDate موجودة أو قابلة للتطبيق',
         typeof formatDate === 'function' || 
         typeof Date !== 'undefined',
         'لتنسيق التاريخ');
    
    // ═══════════════════════════════════════════════════════════════
    // النتائج النهائية
    // ═══════════════════════════════════════════════════════════════
    
    console.log('\n═══════════════════════════════════════════════════════════════');
    console.log('📊 ملخص النتائج:');
    console.log('═══════════════════════════════════════════════════════════════');
    console.log(`✅ الاختبارات الناجحة: ${passedTests}`);
    console.log(`❌ الاختبارات الفاشلة: ${failedTests}`);
    console.log(`📊 المجموع: ${passedTests + failedTests}`);
    
    const percentage = ((passedTests / (passedTests + failedTests)) * 100).toFixed(2);
    console.log(`📈 نسبة النجاح: ${percentage}%`);
    
    console.log('═══════════════════════════════════════════════════════════════');
    
    if (failedTests === 0) {
        console.log('🎉 تهانينا! جميع الاختبارات نجحت!');
        console.log('✅ نظام التسليم جاهز للاستخدام');
        console.log('\n💡 للاختبار اليدوي:');
        console.log('   1. انتقل إلى صفحة "إدارة الصيانة"');
        console.log('   2. اختر جهاز واضغط "تسليم جهاز"');
        console.log('   3. املأ البيانات واضغط "تأكيد التسليم"');
        console.log('   4. تحقق من حفظ البيانات وتحديث الإحصائيات');
    } else {
        console.warn('⚠️ بعض الاختبارات فشلت!');
        
        if (warnings.length > 0) {
            console.log('\n📝 التحذيرات والحلول:');
            warnings.forEach((warning, index) => {
                console.warn(`   ${index + 1}. ${warning}`);
            });
        }
        
        console.warn('\n📝 خطوات الإصلاح:');
        console.warn('   1. تأكد من إضافة ملف maintenance-delivery-fix.js');
        console.warn('   2. تأكد من أن الملف في نفس مجلد index.html');
        console.warn('   3. أعد تحميل الصفحة (Ctrl+Shift+R)');
        console.warn('   4. امسح الـ cache (Ctrl+Shift+Delete)');
        console.warn('   5. أعد تشغيل التطبيق');
    }
    
    console.log('\n═══════════════════════════════════════════════════════════════');
    console.log('شركة الإبداع الرقمي - Digital Creativity Company');
    console.log('© 2025 جميع الحقوق محفوظة');
    console.log('═══════════════════════════════════════════════════════════════');
    
    // إرجاع النتائج للاستخدام البرمجي
    return {
        passed: passedTests,
        failed: failedTests,
        total: passedTests + failedTests,
        success: failedTests === 0,
        percentage: percentage,
        warnings: warnings,
        canTest: window.maintenanceData && window.maintenanceData.length > 0,
        deviceCount: window.maintenanceData ? window.maintenanceData.length : 0,
        deliveredCount: window.deliveredDevicesData ? window.deliveredDevicesData.length : 0
    };
})();
